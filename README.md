# OGG to WAV Converter Microservice

Docker container that converts base64-encoded OGG files to WAV format.

## Usage

### Run Container
```bash
docker run -p 5000:5000 yourusername/ogg2wav-converter:latest